# Current Task

## Goal
Implement P3-01: contract freeze and compatibility bridge. Stop before Prisma foundation.

## Source of Truth
- `docs/00-INDEX.md`, `docs/01-tech-stack.md`, `docs/02-data-schema.md`.
- `docs/07-user-stories-phase3.md` US-3.8, `docs/09-non-functional-requirements.md`, `docs/10-out-of-scope.md`.
- `docs/03-file-structure.md`, `docs/08-effects-and-scenes.md` 8.1–8.7, `docs/11-phase3-technical-roadmap.md` 9.2.
- Workspace runbook `../phase3-execution-plan.md` P3-01.

## Current Decisions
- P3-00 is committed at `b3872b7`; user confirmed CI/Vercel passed on 2026-09-05.
- Use portable Node 24.20.0 in workspace `.tools` for local verification; global Node remains 25.5.0.
- Preserve the pre-existing user edit to `content/stories/demo-story.json` (added looping bg_color_shift to ch1-block-002).
- Keep JSON production source and legacy editor/Reader behavior; target contracts are separate required snapshot types.
- Zod 4.5.4 added as the only direct dependency for this stage.

## Completed
- [x] Freeze the 25-effect technical manifest, separate client icon mapping and presentation seed, retain `EFFECT_METADATA`, and add serializable Admin/Author projection.
- [x] Add strict EffectConfig and Scene snapshot validation with Zod 4.5.4.
- [x] Add canonical Scene target types and separate required `LegacyScene` compatibility types.
- [x] Add typed linear/radial gradient bridge, particle registry, palette tint migration, preset/custom snapshot mappers, and copy-isolation checks.
- [x] Make `SceneLayer` accept only `SceneRenderConfig`; legacy Reader/Preview resolve through the bridge before rendering.
- [x] Add story-bound Scene repository contracts, public chapter aggregate with snapshot scenes, standard API result types, and command-service interfaces.
- [x] Keep JSON as production persistence and legacy editor write path; snapshot reads never resolve live catalog ingredients.
- [x] Add Effect/Scene/repository tests, including every current Scene seed shape.
- [x] Frozen install, lint, typecheck, test, and production build pass on Node 24.20.0 / pnpm 10.33.0.

## In Progress
- None. P3-01 is complete locally.

## Remaining
- Stop before P3-02 Prisma foundation.

## Modified Files
- Effect manifest/schema/projector and compatibility catalog under `src/lib/effects`.
- Canonical/legacy Scene types, parsers, mappers, renderer bridge, repositories, service/API contracts, and tests under `src`.
- `docs/02-data-schema.md`, `docs/08-effects-and-scenes.md`, `docs/verification/p3-01.md`, `package.json`, and `pnpm-lock.yaml`.
- `AGENTS.md` contains the Next.js 16 agent-rules block generated by `next dev`; relevant local Next docs were reviewed after generation.
- Pre-existing user edit to `content/stories/demo-story.json` remains untouched by P3-01.

## Known Issues
- `docs/14-ai-assistant-guidelines.md` is referenced by INDEX but absent throughout workspace; follow available AGENTS/docs.
- Missing legacy `/scene-backgrounds/rain-loop.mp4` remains a P3-04 migration dry-run blocker. P3-01 validates the URL/snapshot shape and provides a media existence audit helper without changing the production JSON read path.
- pnpm's existing warning for the ignored `unrs-resolver@1.12.2` lifecycle script remains; all gates pass with the existing policy.

## Next Step
After P3-01 review/commit, begin P3-02 PostgreSQL + Prisma foundation.

## Last Verified
2026-09-05: P3-01 frozen install, lint, typecheck, tests, and production build passed on Node 24.20.0. P3-00 remote CI/Vercel gate was confirmed by the user.
