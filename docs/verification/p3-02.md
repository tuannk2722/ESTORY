# P3-02 verification — 2026-09-06

P3-02 code and its local/Neon development gates passed. Application persistence
remains JSON. This record does not claim a GitHub Actions or Vercel deployment
has run for the current changeset.

## Scope and implementation

- Starting HEAD: `3972d53`, clean tree; P3-01 at `78de862`.
- Node **24.20.0**, pnpm **10.33.0**, Prisma CLI/client/adapter-pg **7.10.0**,
  PostgreSQL driver **8.23.0**. Prisma 8's registry tag was an RC; selected stable 7.
- `prisma/schema.prisma` maps all 19 documented models and five enums in roadmap
  §9.4/9.4b/9.4c. An automated declaration comparison against those code blocks
  passed after excluding added query indexes and formatting/comments.
- Initial migration `20260906000000_phase3_foundation`: 19 tables, 37 explicit
  indexes (including unique indexes), 19 foreign keys; DDL wrapped in a transaction.
- Server-only singleton, startup env validation and four database JSON codecs.
  Existing Scene/Background Zod validators are reused. No repositories, routes,
  Reader UI, legacy content or manifest definitions were cut over to Prisma.
- Install/build generate the ignored client; deployment of migrations is separate.
  [Operations and rollback](../../prisma/README.md) describe environment separation,
  shadow database use, initial verification and failed-migration recovery.

## Local checks

| Check | Result |
| --- | --- |
| `pnpm install --offline --frozen-lockfile` | Pass; lockfile current, client generated |
| `pnpm db:format` | Pass |
| `pnpm db:validate` | Pass |
| Prisma generate (postinstall and build) | Pass without database credentials |
| `pnpm lint` | Pass |
| `pnpm typecheck` | Pass |
| `pnpm test` | Existing Reader/Effect/Scene/repository tests and new env/JSON tests pass |
| `pnpm build` | Pass; all existing routes built |
| `pnpm test:client-bundle` | Pass; 27 browser JS files contain no Prisma/database-env markers |
| Production startup without URLs | Home responds HTTP 200 using JSON |
| Production startup with unsupported Prisma flag | Env validation fails; requests receive no successful response |
| Credential audit | Supplied URLs/passwords absent from git-visible files; `.env.local` ignored |
| `git diff --check` | Pass |

An initial startup smoke incorrectly expected Next.js to terminate the process
on instrumentation failure. Rechecking through an HTTP request confirmed the
actual fail-closed behavior: env validation rejects the flag and blocks requests.

The existing `unrs-resolver` ignored lifecycle-script warning remains. The Node
native TypeScript preflight loader emits a module-detection warning; it completes
successfully. The pinned pg driver warns about future SSL-mode behavior; the Neon
URLs retain their SSL options and certificate verification was not disabled.

## Neon development verification

The user supplied pooled/direct URLs for the requested empty development database.
They were initially placed in `.env.example`, then moved into ignored `.env.local`;
the committed example contains empty values. Neither credentials nor full URLs
are recorded here. URL target comparison confirmed the same endpoint/database
after removing Neon's pooled hostname suffix.

| Check | Result |
| --- | --- |
| `pnpm db:check-empty` via direct URL | Pass before deployment: no user tables, views or sequences |
| First `pnpm db:migrate:deploy` | Initial migration applied successfully |
| Second `pnpm db:migrate:deploy` | No pending migrations; no duplicate application |
| `pnpm db:migrate:status` | Database schema up to date |
| `prisma migrate diff --from-config-datasource --to-schema prisma/schema.prisma --exit-code` | No difference, exit 0 |
| `pnpm test:db` via pooled URL | Pass: all 19 models, defaults, unique keys, JSON round trips, provenance `SetNull`, delete cascades, transaction rollback |

The integration test deliberately throws at the end of its transaction and
verifies that the fixture user does not remain. It imports no legacy JSON content
into Neon. The database now holds the initial schema and Prisma migration history.
`activatedAt` archive preservation is tested as a database update; enforcement of
set-once/hard-delete/ownership rules remains in the future command-service stages.

## Environment work not claimed as completed

- Current changeset has not been pushed; the new disposable PostgreSQL 17 CI job
  and the updated build job have not been observed on GitHub Actions.
- Vercel Preview/Production deployments and their separately scoped Neon
  credentials/branches have not been configured or verified in this task.
- No production migration, JSON data import, Auth setup, backup restore drill,
  or read/write cutover was performed. P3-03 and later stages remain separate.

P3-04's known missing `/scene-backgrounds/rain-loop.mp4` is unchanged.
